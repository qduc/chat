// Test stubs for health endpoint observable behaviors

describe('GET /healthz', () => {
  test.todo('responds with 200 and { status: "ok" }');
  test.todo('includes service metadata: provider, model, uptime');
  test.todo('includes persistence flags: enabled and retentionDays');
});
